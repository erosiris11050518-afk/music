# Brand Asset

Replace `logo-title.png` in this folder with a transparent horizontal logo image.

Display size in the app: `220 x 36px`.

Recommended source size: `440 x 72px` or larger at the same 6.11:1 ratio.

If `logo-title.png` is missing, the app falls back to `logo-title.svg`.
